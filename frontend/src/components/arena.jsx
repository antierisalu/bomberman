import { LAR } from "../framework"

const Arena = () => {

    const deltaTime = 0;

    return (
        <div>
            <h1>DeltaTime: {deltaTime}</h1>
        </div>
    )
}

export default Arena;